# Real-CUGAN 批处理配置
# 修改下面内容后保存，双击 run_async_v3.bat 运行

# 输入输出目录（相对于项目根目录，无则自动创建）
输入 = _test_in
输出 = _test_out

# 模式：视频 或 图片
模式 = 视频

# 模型选择: pro-conservative-up2x, v3-conservative-up2x 等
模型 = pro-conservative-up2x
放大倍数 = 2
AMF质量 = 18
输出后缀 =

# 分块模式（0=不分块, 2=2x2块/推荐, 值越大块越小显存越低）
分块模式 = 0

# 跳过相邻重复帧（动漫大量重复帧，是=跳过，显著减少 GPU 工作量）
去重 = 是

# 启用 torch.compile 编译优化（首次运行慢，后续可能有提升，否=禁用）
编译 = 否

# 导出详细性能日志（是=生成日志文件到输出目录）
日志 = 否
